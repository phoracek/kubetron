kind: Pod
apiVersion: v1
metadata:
  name: example-kubetron-pod
  annotations:
    kubetron.network.kubevirt.io/networks: red, blue
spec:
  containers:
    - name: example-pod
      image: alpine
      command: ["sleep", "9999"]
  restartPolicy: Never
